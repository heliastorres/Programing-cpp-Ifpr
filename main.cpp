#include <stdio.h>

int main() {
    int A, B, X;

    
    // Lendo os dois valores inteiros
    scanf("%d %d", &A, &B);

    // Calculando a soma
    X = A + B;

    // Imprimindo o resultado conforme o formato pedido
    printf("X = %d\n", X);

    return 0;
}
